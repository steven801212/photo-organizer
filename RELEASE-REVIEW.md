# 1.0.0 發布前 Review

日期：2026-09-05。受審版本：0.9.0b7。

原始審查結論：Beta 7 不通過發布門檻。以下保留原始缺陷證據；修復狀態請以本段更新為準。

## 1.0.0 修復更新

2026-09-05 已完成四項重現缺陷與其餘列出的修復。新的回歸測試納入一般測試集，包括不可覆寫落檔、資料庫還原等待連線、同圖庫操作序列化、ExifTool 逾時及恢復、計畫只載入一次。相簿、回收站、地圖與同地點照片的分頁也已補齊。

另以實際 Chromium 對暫存本機服务驗證登入、舊設定相容、圖庫與相簿翻頁、地圖群組翻頁、大圖 EXIF、方向鍵、快取標頭、登出清空及切換使用者。詳細的最終測試結果收錄於 `artifacts/release-validation.txt`。

版本已升為 1.0.0。驗證涵蓋本機程式及瀏覽器，不包含直接在使用者 NAS 部署或 Docker daemon 建置；不應解讀為已驗證所有 NAS 硬體或斷電情境。

## 已重現的問題

### P1：RAW 匯入可能覆寫既有 XMP

位置：`src/photo_organizer/engine.py:318`、`:274`、`:259`。

RAW 主檔只檢查主檔目的路徑，XMP 則直接使用同主檔名的附檔名路徑。若目的地已存在 XMP，`replace()` 會覆寫該檔，複製模式的最後一步也同樣覆寫。

重現：建立待匯入 `photo.arw` 與新的 `photo.xmp`，並在目的地預先放入不同內容的 `photo.xmp`。匯入後舊 XMP 已變成新檔內容。相機 RAW 格式改變但沿用主檔名時，也可能碰到此情況。

建議：為主檔及附屬檔共同保留不衝突的名稱，且最終落檔操作必須拒絕覆寫。不要只在預覽階段檢查檔名。

### P1：Quick Hash 碰撞後，非重複照片仍進入重複資料夾

位置：`src/photo_organizer/engine.py:205` 至 `:211`。

同批檔案大小與頭尾指紋相同時，程式會比較全文 SHA-256，但目的地改成 `_Duplicates` 的敘述位於 `if prior` 之外。即使全文不同、動作仍是 IMPORT，目的地也已變成重複區。

重現：兩個檔案頭尾各 64KB 相同，中間資料不同且大小相同。兩份全文 Hash 不同，兩筆動作皆為 IMPORT，但其中一筆目的地位於 `_Duplicates`。

建議：只有全文 Hash 確認相同後才能同時修改動作與目的地，並驗證重複檔參照的來源仍存在。

### P1：預覽後變更的檔案可以使用過期分類結果搬移

位置：`src/photo_organizer/engine.py:297` 至 `:303`。

新檔通常在預覽時沒有全文 Hash。預覽後即使大小或修改時間改變，只要目前穩定性檢查通過，就會接受新內容，繼續使用舊拍攝日期、目的地及配對結果。

重現：掃描新 JPG 後，以不同內容取代來源，再執行原計畫。檔案仍成功移入圖庫，來源被移除。

建議：發現來源狀態與預覽不同就拒絕該筆並要求重新掃描。全文 Hash 與搬移前後的穩定性檢查也需涵蓋持續上傳的情境。

### P1：Live Photo 同名配對會壓過明確的 Apple 識別碼

位置：`src/photo_organizer/engine.py:225` 至 `:240`。

掃描先處理同檔名群組，再處理識別碼群組。兩個同名檔就算有不同的識別碼仍會配對，配好後正確識別碼群組因來源已被配對而跳過。

重現：`photo.jpg` 識別碼 A、`photo.mov` 識別碼 B、`matching.mov` 識別碼 A。程式把 JPG 與 B 配在一起，而 A 的 MOV 沒有配對。

建議：明確識別碼優先；識別碼衝突禁止同名回退；多個候選存在時不得任意選第一筆。

## 靜態審查的其他發布阻礙

### P1：備份還原未與所有資料庫存取互斥

位置：`src/photo_organizer/service.py:490`、`:526`、`:536`；`src/photo_organizer/engine.py:758`。

還原只讀取一次 `state.busy`，未持鎖或占用工作狀態。其他瀏覽、最愛、回收站、排程清除及新的背景工作可以在還原期間開啟或寫入相同資料庫。還原函式直接移除 WAL/SHM 並替換主資料庫，不保證其他連線已關閉。這會造成還原失敗、寫入落在舊檔或狀態不一致的風險。

此項是程式路徑審查結果，尚未在 Synology 上進行併發故障注入。應先建立每位使用者一致的維護互斥機制，再測試還原與並行操作。

### P1：同一瀏覽器切換帳號可能顯示前一帳號的圖片快取

位置：`src/photo_organizer/service.py:236`；`src/photo_organizer/web/app.js` 的縮圖及預覽 URL。

不同使用者有獨立資料庫，照片 ID 會重複，但 URL 都是 `/api/thumbnail/1` 或 `/api/preview/1`。回應允許瀏覽器私有快取 24 小時，未以帳號區分 URL 或設定 `Vary: Cookie`。切換帳號後，瀏覽器可能直接重用先前圖片，不再向伺服器驗證。退出登入也沒有清空現有圖庫及預覽內容。

此項依回應標頭與前端路徑確認風險，尚未以實際瀏覽器跨帳號驗證。应使用帳號隔離的快取識別與合適快取策略，登出時清除畫面狀態。

### P2：每次進度查詢都重新解析完整掃描計畫

位置：`src/photo_organizer/service.py:95` 至 `:96`。

`STATES.setdefault(username, State(...))` 的預設值會先求值。因此即使 State 已存在，每次呼叫仍建立一個新的 State 並讀取完整 scan-plan.json，之後才丟棄。網頁每秒查詢狀態、每張 Inbox 縮圖也會呼叫此函式，數萬筆預覽時會放大 I/O、記憶體與 CPU 開銷。

建議：鎖內先檢查字典，只有首次建立時才讀取持久化計畫。需要大量清單測試確認讀取次數。

### P2：ExifTool 常駐讀取缺少工作逾時

位置：`src/photo_organizer/metadata.py:34` 至 `:50`。

stdout.readline() 無期限等候 ready 標記，期間持有全域 metadata 鎖。一個卡住的檔案可能阻塞其他使用者的掃描、重新索引與 EXIF 預覽；目前 fallback 只有在拋出例外後才會發生，無法解除永久等待。

建議：為每個工作加入可中止的逾時、終止及重建進程，並測試永不回應的模擬進程。

## 其他已看到的功能缺口

- 自動整理設定在 Python 儲存為 `True`，網頁用小寫 `true` 比較，會顯示未勾選；排程卻仍會執行。
- 相簿目前只載入前 60 張、回收站只列前 200 筆、地圖只取得前 5000 筆。同地點只提供前 30 張選取，尚缺完整分頁與總量說明。
- 重新索引仍在寫入交易內讀取 EXIF；與先前宣稱的 metadata 在交易外處理不一致。
- ZIP 下載建立暫存檔後未關閉 mkstemp 回傳的檔案描述元，長期服務會累積描述元，Windows 也可能無法清理暫存 ZIP。

## 原始 Beta 7 審查驗證紀錄（修復前）

- 原有帳號、整理引擎、圖庫資料庫與 metadata 共 28 項測試通過。
- 新增 `tests/review_release_gate.py` 的四項安全期望測試全部失敗，對應以上四個已重現缺陷。它們用 mock metadata 與暫存檔隔離驗證整理邏輯，不讀取或變更真實照片。
- 執行方式：將 `src` 加入 PYTHONPATH，再執行 `python tests/review_release_gate.py`。
- 本次未執行完整 NAS/Docker 測試、真實瀏覽器測試、Pillow/HEIC 測試或斷電恢复測試，因此不能把結果當成完整生產環境認證。
- 未升版，未產生 1.0.0 ZIP。應完成修正及對應回歸後，再作發布決定。
