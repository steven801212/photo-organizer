# Photo Organizer 1.0.1

[English](RELEASE-NOTES.en.md) | **繁體中文（台灣）**

發布日期：2026-09-08。適用於私人／家庭內網的單一 Synology Organizer Service，支援多位使用者各自獨立的圖庫與索引。

## 從 1.0.0 或 Beta 7 升級

1. 等候正在執行的整理工作完成，先在管理中心備份索引。
2. 在 Container Manager 停止 `photo-organizer` 專案。
3. 將 1.0.1 ZIP 解壓到 `PhotoOrganizer/app`，更新程式檔案。保留 NAS 原本的 `.env` 管理員密碼與 PUID/PGID；若原有 compose 使用自訂掛載位置或連接埠，將它們保留到新設定。
4. 新 compose 映像標籤為 `photo-organizer:1.0.1`。重新建置映像並建立容器；只重啟舊容器不會更新程式。
5. 重新開啟網頁，必要時按 Ctrl+F5，確認左上角顯示 `v1.0.1`。
6. 升級後重新掃描 Inbox，確認最新預覽再執行整理。既有照片不會因升級而自動重新搬移。

不要刪除 `PhotoInbox`、`PhotoLibrary`、`PhotoOrganizer/data`、`backups` 或 `rejected`。

若使用 SSH／終端機，可在 app 目錄執行：

```sh
docker compose build --pull photo-organizer
docker compose up -d --force-recreate photo-organizer
```

這些命令會更新服務容器，掛載的照片與資料庫資料夹不會被清除。

## 1.0.1 介面更新

- 照片、年份與日期由近到遠排列；日期未知項目置底，相同時間的照片維持穩定分頁順序。
- 年份資料夾預設收合，點擊或使用鍵盤才展開日期；同一次瀏覽保留手動展開的狀態。
- 移除主畫面固定最大寬度與垂直置中，依可用視窗寬度自動延展，照片欄數自動調整。
- 工具列在較窄視窗換行；匯入預覽可用高度隨視窗調整，年份列表過長時可獨立捲動。
- 本次只調整瀏覽排序與顯示，不搬動照片、不變更既有資料夾結構，也不需要重新索引。

## 延續 1.0.0 正式版修復

- 修復 XMP 覆寫、錯誤重複分類、過期預覽與 Live Photo 配對問題。
- 保護備份還原與資料庫存取，避免服務內的並行操作互相干擾。
- 登出清除圖庫與大圖，圖片快取依登入與內容隔離。
- 改善大量 Inbox 的進度查詢，讀取卡住時可逾時回復。
- 補齊相簿、回收站、地圖和同地點照片的分頁。
- 回收站自動永久清除預設關閉，只有明確勾選後才依保留天數執行。

## 使用範圍

每組資料夾只啟動一個 Organizer Service。多台 PC 使用瀏覽器連到同一個 NAS 服務；不要讓另一個容器或 CLI 同時整理相同圖庫。

目前驗證環境為 Windows 本機 Python 與實際 Edge（Chromium）。48 項回歸測試通過；瀏覽器測試涵蓋 1280×800、1920×1080、2560×1440、3840×2160 的版面、年份與日期排序、收合與展開、登入切換、預覽及分頁。此工作環境沒有 Docker daemon，也未直接更新你的 NAS，因此 Synology 容器的最終啟動狀態須以你部署後顯示的版本及服務狀態為準。

地圖元件內建 Leaflet 1.9.4，授權文件隨程式提供；底圖仍由 OpenStreetMap 按正常瀏覽需求載入。照片留在 NAS，不提供離線底圖下載。
