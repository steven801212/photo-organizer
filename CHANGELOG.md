# Photo Organizer 版本紀錄

## v1.0.1 — 2026-09-08

- 修正前端物件鍵排序造成的年份由舊到新；年份、日期與照片統一由近到遠，日期未知項目置底。
- 年份資料夾預設收合，支援滑鼠與鍵盤展開；箭頭隨展開狀態切換。
- 圖庫同一時間的照片增加穩定排序，避免分頁時順序不明確。
- 主畫面靠上排列、取消 1500px 寬度限制，依可用視窗寬度延展並自動調整照片欄數。
- 工具列支援換行，匯入預覽高度與年份捲動區依視窗高度調整。
- 新增日期排序及收合操作回歸測試，驗證 1280×800 至 3840×2160 的桌面版面。
- 不更動照片、索引結構或實體資料夾，不需要重新索引。

## v1.0.0 — 2026-09-05

- 修正 RAW/XMP 目的檔衝突；主檔與側載檔共同避開已存在的名稱，正式落檔拒絕覆寫。
- 同磁碟區先建立硬連結，索引提交後才移除來源；不支援時回退複製驗證。
- 修正 Quick Hash 碰撞但全文 SHA-256 不同時，照片被放入重複區的錯誤。
- 來源大小或修改時間與預覽不一致時停止該筆，並要求重新掃描。
- Apple Live Photo 識別碼優先於檔名，識別碼衝突或候選不唯一時不任意配對。
- 備份還原等待現有資料庫連線關閉；同一位使用者的檔案維護操作依序執行。
- 登出清除圖片與頁面資料；媒體請求避開舊帳號快取，縮圖快取名稱包含照片內容雜湊。
- 修正每次查詢進度都重新解析整份掃描清單的效能問題。
- ExifTool 常駐讀取加入逾時終止與重新啟動；版本檢查不再每秒啟動外部程式。
- 重新索引在寫入交易外讀取 metadata；新增資料庫路徑隔離檢查。
- 相簿、回收站及地圖提供載入更多，同地點照片提供分頁；超過批次上限時明確提示。
- 修正自動整理設定的大小寫相容；回收站自動永久清除改成獨立、預設關閉的選項。
- 修正 ZIP 暫存檔描述元未關閉，以及永久清除被重複紀錄外鍵阻擋的問題。
- 固定影像相依套件版本，新增 Docker 建置排除清單及 Leaflet 授權文件。

## v0.9.0 Beta 7 — 2026-08-22

- 「拍攝地圖」改為預設顯示完整互動式地圖，不再只是照片清單與外部連結。
- 使用內建 Leaflet 與 OpenStreetMap 底圖，無須 Google Cloud 帳單或 Apple Maps Token。
- GPS 照片以圓形縮圖標示；同一座標合併顯示數量，展開後可選擇照片。
- 點擊地圖照片會直接開啟既有的大圖、滾輪縮放、方向鍵切換及 EXIF 資訊介面。
- 地圖會自動縮放到所有拍攝地點，並提供「顯示所有地點」按鈕。

## v0.9.0 Beta 6 — 2026-08-22

- 修正匯入流程誤用狀態 API 前 500 筆預覽，導致單次只整理 500 個檔案的問題。
- 後端改為直接處理完整掃描計畫，不再具有 500 個檔案的整理上限。
- 匯入預覽每頁最多 500 筆，加入上一頁／下一頁及頁數顯示。
- Inbox 縮圖 API 使用完整清單的絕對索引，分頁後仍能取得正確預覽。

## v0.9.0 Beta 5 — 2026-08-22

- 底層排除 Synology `@eaDir`/`@tmp`、macOS `.DS_Store`/AppleDouble、Windows `Thumbs.db`/`desktop.ini` 及常見未完成暫存檔。
- ExifTool 改用 `-stay_open` 常駐管道並串行批次讀取，異常時自動重啟或回退單次呼叫。
- 重複候選先比較檔案大小與 64KB 頭尾快速指紋，最終判定仍使用完整 SHA-256。
- SQLite 保留 WAL 並新增 10 秒 busy timeout；metadata 讀取移到寫入 transaction 之外。
- HEIC/RAW 縮圖預設限制為單一解碼工作，可以 `THUMBNAIL_WORKERS` 調整 1–4。
- 新增選用 PUID/PGID 降權執行、同／跨磁碟區檢測與 UI 警示。
- 工作狀態與掃描計畫持久化，服務重啟後可辨識中斷工作。
- 日期回溯新增 XMP DateCreated 與常見檔名日期，保持字面本地時間，不強制轉 UTC。
- Live Photo 優先使用 Apple ContentIdentifier/MediaGroupUUID，再回退同資料夾同檔名配對。
- 目的檔名轉為 Unicode NFC，並安全處理 Windows/SMB 不相容字元與保留名稱。

## v0.9.0 Beta 4 — 2026-08-22

- 掃描與整理新增分階段進度、已處理／總檔案數、當前檔名與安全停止。
- 非照片檔案改存每位使用者獨立的 `PhotoOrganizer/rejected` 隔離區，保留原始子資料夾結構。
- 非照片檔以 SHA-256 判斷內容重複，重複項完整保留於 `_Duplicates`。
- 整理完成後由最深層開始清除 Inbox 空子資料夾，不刪除使用者 Inbox 根目錄。
- RAW 的 XMP 側載檔仍跟隨 RAW，不會被當成其他檔案。

## v0.9.0 Beta 3 — 2026-08-22

- 內建 HEIC/HEIF 解碼器，不再只依賴檔案內嵌的 JPEG 預覽。
- HEIC/HEIF 縮圖與 1600px 放大預覽會轉為瀏覽器通用的 sRGB JPEG。
- Live Photo 圖庫縮圖保持靜態；開啟放大預覽後可切換播放配對的 MOV。

## v0.9.0 Beta 2 — 2026-08-22

- 新增獨立 `LIVE/年份/日期` 分類；新匯入的同檔名 JPG/HEIC + MOV 成組存放。
- 重新索引可配對既有分散於 JPG 與 VIDEO 的 Live Photos，但不會自行移動。
- 管理中心新增既有 Live Photos 搬移預覽與明確確認，執行前自動備份索引。
- 圖庫只顯示主照片並加入 LIVE 標示、MOV 播放控制及 Live Photos 篩選。
- 下載、回收站、還原與永久清除會將主照片及 MOV 成組處理。
- MOV 播放支援 HTTP Range，不必先將整段影片載入記憶體。
- 發生同名衝突時，照片與 MOV 使用一致的新主檔名，避免配對失聯。

## v0.9.0 Beta 1 — 2026-08-22

- 整合檔名、日期、相機、鏡頭、ISO 與焦距快速搜尋。
- EXIF 摘要與 GPS 座標寫入每位使用者自己的索引；重新索引可補齊舊照片。
- 新增私人相簿、最愛、多選與最多 200 個檔案的 ZIP 下載。
- 新增拍攝位置瀏覽並連結 OpenStreetMap，不建立跨使用者共用圖庫。
- 新增安全回收站、批次還原、保留天數與明確確認後的過期清除。
- 新增資料庫完整性、遺失檔案、未索引檔案健康檢查及安全修復。
- 新增索引備份版本清單、還原前自動安全備份及還原後完整性驗證。
- 每次正式匯入前後皆自動建立索引備份。
- 新增定時掃描、可選自動整理及 Webhook 完成／失敗通知。
- 新增管理員帳號停用、啟用、密碼重設與每位使用者圖庫容量統計。
- 新增資料庫遷移紀錄，舊版索引可保留照片紀錄並自動升級。
- 整理前顯示檔案數、容量、新照片、重複照片及無日期照片確認摘要。
- 手機專用介面、PWA 與家庭共用圖庫不包含在此版本。

## v0.4.0 — 2026-08-22

- 圖庫新增檔名與日期搜尋、格式篩選及只看最愛。
- 照片可標記最愛，狀態儲存在每位使用者自己的資料庫。
- 新增照片多選、選擇目前載入照片與批次下載 ZIP，單次上限 200 個檔案。
- ZIP 在 NAS 暫存並以串流下載，完成後立即移除暫存檔。
- 現有資料庫啟動時自動加入最愛欄位，不需要重建索引。

## v0.3.0 — 2026-08-22

- 照片圖庫新增「年份 → 日期」分層分類、數量統計與篩選。
- Inbox 掃描結果新增 JPG、RAW 等格式的縮圖預覽。
- 放大預覽改用 1600px 圖片，支援滾輪縮放、雙擊還原及方向鍵切換照片。
- 檔名、拍攝時間、機身、鏡頭、光圈、快門、ISO、GPS 等資訊集中於右側面板。
- 支援 ICC 色彩描述檔轉換至 sRGB，並避免瀏覽器拖曳選取造成藍色覆蓋。
- 新增使用者及首次安裝密碼改為只限制不可空白。
- Docker 映像改用明確版本標籤，避免 NAS 更新時沿用舊映像。

## v0.2.1 — 2026-08-21

- 修正更新容器時仍重複驗證首次安裝密碼，導致既有服務無法啟動的問題。
- 既有帳號資料庫存在時，不再使用 Compose 密碼覆蓋或阻擋啟動。
- 新安裝仍要求管理員密碼至少 10 個字元。

## v0.2.0 — 2026-08-21

- 新增登入後的照片圖庫縮圖瀏覽、格式篩選與放大預覽。
- 新增最近 100 筆操作紀錄頁。
- 新增 MOV / MP4 分流至 `VIDEO/年份/YYYY-MM-DD`。
- 新增 RAW 同名 XMP sidecar 跟隨移動與 Undo。
- 同一 NAS Volume 改用快速原子搬移，跨裝置仍使用複製＋SHA-256 驗證。
- 移除預覽後每張照片重複等待 10 秒的問題。
- 整理完成後顯示成功與失敗數。
- 左上角顯示程式版本，並停用前端舊檔快取。

## v0.1.0 — 2026-08-21

- Synology Container Service 與 macOS 風格 Dashboard。
- 多使用者獨立 Inbox、圖庫、SQLite 與備份。
- EXIF 日期、RAW/JPG 分流、SHA-256 去重及 Manifest。
- 索引備份、重新索引、登入與管理員新增使用者。
